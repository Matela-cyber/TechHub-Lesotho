// Firebase completely removed from admin side. No initialization or config required.
// If you need to re-enable Firebase, restore the previous code and add your config.

// Dummy exports to prevent import errors elsewhere in the codebase
export const auth = null;
export const db = null;
